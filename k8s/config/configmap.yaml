apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  POSTGRES_HOST: postgres
  POSTGRES_PORT: "5432"
  VITE_PROXY_TARGET: "http://backend:8000"
  # VITE_CLIENT_ID will be added from secrets

